import Container from "@/components/layouts/Container";

const NavbarContainer = () => {
  return (
    <header>
      <Container>NavbarContainer</Container>
    </header>
  );
};

export default NavbarContainer;
